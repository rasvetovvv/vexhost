POSTGRES_USER=vexhost
POSTGRES_PASSWORD=change-me
POSTGRES_DB=vexhost
CORS_ORIGINS=https://host.vexory.xyz
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=
DASHBOARD_URL=https://host.vexory.xyz/?view=dashboard
# Long random string used to sign web session tokens (e.g. `openssl rand -hex 32`).
AUTH_SECRET=
# Shared secret for backend -> runtime-manager API (e.g. `openssl rand -hex 32`).
RUNTIME_MANAGER_TOKEN=
